function fn(number) {
  return number * 2;
}

const fn = function (number) {
  return number * 2;
};

console.log(fn(2));

console.log(2);

const fn = (number) => {
  return number * 2;
};

console.log(2);

const FnArrow = (number) => number * 2;
