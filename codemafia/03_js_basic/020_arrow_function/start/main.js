function fn(number) {
  return number * 3;
}
console.log(fn(2));

const fn = function (number) {
  return number * 2;
};

const fnArrow = (number) => {
  return number * 3;
};
console.log(fnArrow(3));

<div className="."></div>;
